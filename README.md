# my personal arch dotfiles.

run the script with `curl -sSL https://raw.githubusercontent.com/zekurio/dotfiles/main/setup.sh | sh` and let the script do its magic

this only works in linux currently and is tested solely on arch, debian based distros might work.